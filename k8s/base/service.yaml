apiVersion: v1
kind: Service
metadata:
  name: mcp-git-intel
  namespace: mcp-git-intel
  labels:
    app.kubernetes.io/name: mcp-git-intel
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-git-intel
    app.kubernetes.io/component: server
  ports:
    - name: stdio
      port: 3000
      targetPort: 3000
      protocol: TCP
